export type Inputs = {
  firstName: string;
  lastName: string;
  login: string;
  email: string;
  password: string;
  country: string;
  city: string;
  street: string;
  postalCode: string;
  dateOfBirth: string;
};

export type InewValue = {
  value: string;
  label: string;
  className: string;
  pattern: string;
};
